import React, { useEffect } from "react";
// import Form from "../components/form";

const Main = (props) => {
    return (
        <div>
            <p>This is the main feed</p>
        </div>
    );
};

export default Main